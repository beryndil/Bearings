<script lang="ts">
  /**
   * Files tab placeholder — Phase 3 of the v1.0.0 dashboard
   * redesign. The real Files surface (working_dir tree + files
   * touched by this session's tool calls) ships in Phase 5. The
   * tab exists today so the right-sidebar nav lands fully populated
   * and tests against the mockup match.
   */
</script>

<div class="p-4 text-sm text-slate-400" data-testid="inspector-tab-files-placeholder">
  <div class="rounded-md border border-slate-800 bg-slate-950/40 px-3 py-3">
    <p class="font-medium text-slate-300">Files</p>
    <p class="mt-1 text-xs text-slate-500">
      Working_dir tree and per-session files-touched view ship in Phase 5 of the v1.0.0 dashboard
      redesign. Tool calls that touch the filesystem will be summarized here.
    </p>
  </div>
</div>
