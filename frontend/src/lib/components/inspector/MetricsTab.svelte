<script lang="ts">
  /**
   * Metrics tab placeholder — Phase 3 of the v1.0.0 dashboard
   * redesign. Detailed per-turn / per-tool metrics ship in Phase 6
   * alongside the dedicated Analytics page; the basic Session
   * Metrics card (totals input/output/cache) lives in the Context
   * tab today, where it joins the rest of the per-session at-a-
   * glance data.
   */
</script>

<div class="p-4 text-sm text-slate-400" data-testid="inspector-tab-metrics-placeholder">
  <div class="rounded-md border border-slate-800 bg-slate-950/40 px-3 py-3">
    <p class="font-medium text-slate-300">Metrics</p>
    <p class="mt-1 text-xs text-slate-500">
      Detailed per-turn / per-tool metrics ship in Phase 6 of the v1.0.0 dashboard redesign,
      alongside the dedicated <code class="rounded bg-slate-800 px-1 text-emerald-400"
        >/analytics</code
      > page. Basic session totals live on the Context tab.
    </p>
  </div>
</div>
