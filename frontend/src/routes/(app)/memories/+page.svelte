<script lang="ts">
  /**
   * Memories page placeholder — Phase 2c of the v1.0.0 dashboard
   * redesign. The Memories nav item points here so the rail item is
   * functional from day one; the real Memories UI lands in Phase 4
   * of the redesign plan, backed by the existing `tag_memory`
   * system-prompt layer.
   */
</script>

<section
  class="flex h-full flex-col items-center justify-center px-6 text-center"
  data-testid="placeholder-memories"
>
  <div class="rounded-md border border-slate-800 bg-slate-900 px-6 py-5">
    <h1 class="mb-2 text-base font-medium text-slate-200">Memories</h1>
    <p class="max-w-md text-sm text-slate-400">
      Memories ships in Phase 4 of the v1.0.0 dashboard redesign. They will be backed by the
      existing <code class="rounded bg-slate-800 px-1 text-emerald-400">tag_memory</code>
      system-prompt layer, so durable agent context per tag becomes a first-class surface.
    </p>
  </div>
</section>
